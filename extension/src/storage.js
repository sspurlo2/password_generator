// This will kind of be a settings API for our project when we add more options.
// Where we read/write user settings using chrome.storage, so the rest of your code doesn’t touch Chrome APIs directly.
// Use later as import { getSettings, saveSettings } from "./storage.js";